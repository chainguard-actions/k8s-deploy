name: Test canary deploy with imagepullsecrets and traffic-split-annotations
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          manifests: actions/hardened/Azure--k8s-deploy/v5.1.0/tests/fixtures/manifests/deployment.yaml
          images: nginx:latest
          imagepullsecrets: my-registry-secret
          strategy: canary
          action: deploy
          percentage: 20
          traffic-split-method: pod
          traffic-split-annotations: "key1=value1,key2=value2"
          token: ${{ github.token }}
        continue-on-error: true
      - name: Assert action ran (expected to fail without cluster)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "skipped" ]; then
            echo "FAIL: action was skipped unexpectedly"
            exit 1
          fi
          echo "PASS: action ran (outcome=$OUTCOME)"
